
@{
    ViewBag.Title = "GetReviews";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>GetReviews</h2>

<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>

<div class="col-md-10 col-md-offset-1">
    @*<div class="well">
            <input id="btnLoadEmployees" class="btn btn-success"
                   type="button" value="Load Employees" />
        </div>*@


    <ul class="list-items"></ul>
    <div id="listitems">

    </div>

    <script type="text/javascript">
        function reviewTemplate(review) {
            return `

            <article class="media content-section">
                <img class="rounded-circle article-img" src="">
                <div class="media-body">
                    <div id="author&restaurent" class="article-metadata" style="border:none;">
                        <a id="author" class="mr-2" href="#">${review.ApplicationUser}</a> reviewed
                        <a id="restaurent" class="mr-2" href="#">${review.RestaurantOrPalce} </a>
                        <small id="reviewdate" class="text-muted">${review.TimePosted}</small>
                    </div>
                    <div class="card" style="border:none;">
                        <div class="row ">
                            <div id="photo" class="col-md-3">

                                <img src="" class="w-100">

                            </div>
                            <div class="col-md-9 px-1">
                                <div class="card-block px-1">
                                    <h4><a id="title" class="article-title" href=#">${review.PostTitle}</a> </h4>
                                    <p id="body" class="article-content">${review.PostBody}</p>
                                    <div id="tag" class="row">

                                        <p class="article-content">${review.Tags}</p>

                                    </div>

                                    <div class="'row">

                                        <p id="item" class="article-content" ${review.Items}></p>

                                    </div>
                                    <a href="#" class="btn btn-primary">Read More</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </article>

                                    `
        }
    </script>

    <div id="divError" class="alert alert-danger collapse">
        <a id="linkClose" href="#" class="close">&times;</a>
        <div id="divErrorText"></div>
    </div>


    <script src="Scripts/jquery-1.10.2.min.js"></script>
    <script src="Scripts/bootstrap.min.js"></script>
    <script src="Scripts/jquery-1.10.2.min.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            if (sessionStorage.getItem('accessToken') == null) {
                window.location.href = "http://localhost:55407/User/signin";
            }

            $('#linkClose').click(function () {
                $('#divError').hide('fade');
            });

            $('#errorModal').on('hidden.bs.modal', function () {
                window.location.href = "Login.html";
            });

            //$('#btnLoadEmployees').click(function () {
            $.ajax({
                url: 'http://localhost:55407/api/Review/GetReviewPosts',
                method: 'GET',
                headers: {
                    'Authorization': 'Bearer '
                        + sessionStorage.getItem("accessToken")
                },

                success: function (data) {
                    $('#divData').removeClass('hidden');
                    $('#tblBody').empty();


                    console.log(data);
                    

                    document.getElementById("listitems").innerHTML = `

                                ${data.map(reviewTemplate).join('')}

                            `









                },
                error: function (jQXHR) {
                    // If status code is 401, access token expired, so
                    // redirect the user to the login page
                    if (jQXHR.status == "401") {
                        $('#errorModal').modal('show');
                    }
                    else {
                        $('#divErrorText').text(jqXHR.responseText);
                        $('#divError').show('fade');
                    }
                }


            });
        });
    </script>

</div>